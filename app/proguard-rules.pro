# Add project specific ProGuard rules here.
# Minification is disabled by default for this small app (see app/build.gradle.kts),
# so these rules are not currently applied — kept here for future extension.

# Uncomment if you enable isMinifyEnabled = true and see issues with line numbers
# in stack traces from crash reports:
#-keepattributes SourceFile,LineNumberTable
#-renamesourcefileattribute SourceFile
